# Parse logical field and compile data for warning if parsing errors occurred

Parse logical field and compile data for warning if parsing errors
occurred

## Usage

``` r
check_field_is_logical(x)
```

## Arguments

- x:

  vector to parse
