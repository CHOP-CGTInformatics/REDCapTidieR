# Remove html tags and field embedding logic from a string

Remove html tags and field embedding logic from a string

## Usage

``` r
strip_html_field_embedding(x)
```

## Arguments

- x:

  vector of strings to format

## Value

vector of strings with html tags, field embedding logic, and extra
whitespace removed
