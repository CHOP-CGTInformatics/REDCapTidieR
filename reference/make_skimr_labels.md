# Make skimr labels from default skimr outputs

A simple helper function that returns all default `skimr` names as
formatted character vector for use in `make_lablled`

## Usage

``` r
make_skimr_labels()
```

## Value

A character vector

## Details

All labels supplied are manually created and agreed upon as
human-readable
